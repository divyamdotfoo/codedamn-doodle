// This is your canvas router
// All the request regarding canvas(drawing board) should be handle here.
import { router, publicProcedure } from "../trpc";
import { z } from "zod";

export const canvasRouter = router({});
